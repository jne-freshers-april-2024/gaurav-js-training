var age = Math.random()* 100;
if(age>20){
    console.log(age)
}else{
    console.log("age is not defined")
}

let piValue = Math.PI; // print the pi value
console.log(piValue)

let sqrtvalue = Math.sqrt(16); // gives the square root
console.log(sqrtvalue)

let roundVal = Math.round(4.5); // when value is .5 or more than it takes value as max and less than 0.5 takes vales as min
console.log(roundVal)

let floorVal = Math.floor(6.9); // take the value as maximum
console.log(floorVal)

let ceilVal = Math.ceil(12.1); // take the value as minimum
console.log(ceilVal)

let truncVal = Math.trunc(2.1);
console.log(truncVal)

let negVal = Math.abs(-78);
console.log(negVal)

let power = Math.pow(4, 2);
console.log(power)

let sinVal = Math.sin(360);
console.log(sinVal)

let maxVal = Math.max(1, 2, 5, 56, 90);
console.log(maxVal)

let minVal = Math.min(11, 1, 4, 0.4, 0.1);
console.log(minVal)